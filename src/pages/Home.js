import React, { useContext } from 'react';
import { AuthContext } from '../contexts/UserContext';

const Home = () => {
    return (
        <div>
            this is home
        </div>
    );
};

export default Home;